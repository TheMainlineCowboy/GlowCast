// Disabled intentionally.
// The threshold-only cleanup made detection worse by guessing at boxes.
// Keep this file as a no-op so the existing build command still succeeds.
console.log("edge detector cleanup patch disabled");
